Initial QR code:
normal temperature, no symptom ==> green
normal temperature but have symptom ==> yellow (7 quarantine days required)
Abnormal temp ==> red (14 quarantine days required)

For green code:
     if Abnormal temp, code ==> red
     if have symptom, code ==> yellow
For red code:
     keep healthy (normal temp and no symptom) for 14 days ==> green
     have any unhealthy record ==> resume quarantine
For yellow code:
     keep healthy (normal temp and no symptom) for 7 days ==> green
     have symptom ==> resume quarantine
     Abnormal temp, code ==> red
     
Solidity不支持float，前端读体温的时候可以先近似成整数
     
   
    
